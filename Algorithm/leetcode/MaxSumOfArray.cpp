#include <iostream>
#include <vector>
using namespace std;
void MaxSumOfArray(vector<int>& array,int* start,int* end){
	if(array.size() == 0) //数组大小为0，直接退出
		return;
	int sum = array[0];
	int max = array[0];
    
}
int main(){
	
}